name: ADS-B Proxy
# DISABLED — system taken offline
on:
  workflow_dispatch: # manual only, schedule removed

jobs:
  fetch:
    if: false  # disabled
    runs-on: ubuntu-latest
    steps:
      - run: echo "disabled"
